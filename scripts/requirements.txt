# Pepper Network Deal Scraper - Dependencies
# Install with: pip install -r requirements.txt

# Core (all strategies)
# No additional packages needed for Strategy 2 (cookie) and Strategy 5 (discovery)

# Strategy 1: undetected-chromedriver
undetected-chromedriver>=3.5.0
selenium>=4.15.0

# Strategy 3: Google cache (also useful for general HTTP)
cloudscraper>=1.2.71
curl_cffi>=0.5.0

# Strategy 4: Third-party API (uses requests, which is usually pre-installed)
requests>=2.28.0
